on: push
jobs:
  test:
    runs-on: macOS-latest
    steps:
    - uses: actions/checkout@master
    - name: Set XCode Version
      run: sudo xcode-select -s /Applications/Xcode_11.app
    - name: Test
      run: |
        ls -al
        xcodebuild test -project ReCombine-Scoreboard-SwiftUI.xcodeproj -scheme ReCombine-Scoreboard-SwiftUITests -sdk iphonesimulator -destination "platform=iOS Simulator,id=2E172B25-7015-4615-B50E-362277005F3F"

